# Requirements for ai-service/scripts/ (dataset audit + training comparison)
# These are dev-time tools, NOT needed for the deployed AI service itself —
# keep them out of ai-service/requirements.txt so production installs stay lean.

pillow==10.3.0
pyyaml==6.0.1
imagehash==4.3.1
numpy==1.26.4
ultralytics==8.2.103

# needed by generate_hard_negatives.py
opencv-python-headless==4.10.0.84
